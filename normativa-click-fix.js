/* ============================================================
   CENTINELA CODE — COMPATIBILIDAD
   Parche de clics desactivado para preservar el delegado original
   de app.js y evitar interferencias entre módulos.
   ============================================================ */
(function(){
  'use strict';
  // Sin interceptores globales. app.js conserva el control normal.
})();
